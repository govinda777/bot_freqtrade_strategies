
# Fron

## Install

```bash
npm install --registry=https://registry.npmjs.org/
cd frontend && npm install --save-dev @types/jest  --registry=https://registry.npmjs.org/
npm install --save-dev jest-environment-jsdom --registry=https://registry.npmjs.org/
```

## 